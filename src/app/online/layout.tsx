import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "RESET90 Online Programs",
};

export default function OnlineLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
